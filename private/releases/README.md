# Manifest-backed releases only

Place a paper here only after its Question Bank release manifest, Formatter report and named human signoff all identify the same immutable content checksum. Then change the matching catalog entry to `released` and provide `releaseManifest`, `privateFile` and a positive integer `priceRupees`.
